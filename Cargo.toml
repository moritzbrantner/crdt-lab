[package]
name = "crdt-lab"
version = "0.1.0"
edition = "2024"
description = "Deterministic experiments for conflict-free replicated data structures"
license = "MIT OR Apache-2.0"
publish = false

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[lints.rust]
unsafe_code = "forbid"

[lints.clippy]
all = "deny"
pedantic = "deny"
